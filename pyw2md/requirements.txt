# pyw2md项目依赖列表
#
# 此文件包含项目运行所需的所有第三方Python库
# 使用命令安装: pip install -r requirements.txt

# UI框架 - 提供现代化的tkinter界面
customtkinter>=5.2.0

# 文件系统监控 - 监控文件变化
watchdog>=3.0.0

# PyInstaller - 用于打包成可执行文件 (构建时需要)
PyInstaller>=5.0.0

# 兼容性依赖 - 解决Python 3.12+中distutils移除的问题
setuptools>=65.0.0

# 拖放功能支持
tkinterdnd2>=0.3.0

# 开发和测试依赖 (可选)
# pytest>=7.0.0          # 测试框架
# coverage>=7.0.0         # 代码覆盖率